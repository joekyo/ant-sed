JDK 8 and Ant 1.10.x are required

To build my-tasks.jar, run
`ant`

To run all tests, use
`./t.sh`

To run specific test, for example, to test ReplaceLine
`./t.sh replace`

To show debug messages, run
`./t.sh replace -d`
